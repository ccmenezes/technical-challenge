/* Login Page */
//Tests

//Should login with user valid credentials
export const ACCOUNT_PATH = /account/;

//Should throw an error message for invalid credentials
export const INCORRECT_CREDENTIALS_MSG = 'Invalid email or password';
